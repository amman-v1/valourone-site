---
const { name = 'ValourOne', url = 'https://valourone.com' } = Astro.props;
const schema = {
  '@context': 'https://schema.org',
  '@type': 'LocalBusiness',
  name,
  url,
  areaServed: 'GB',
  sameAs: ['https://www.linkedin.com/company/valourone'],
};
---
<script type="application/ld+json">{JSON.stringify(schema)}</script>
